{# Artifact variant: the host supplies <!doctype>, <html>, <head> and <body>,
   so this emits page content only. <title> stays first so it lands inside the
   8KB the publisher scans. #}
<title>{{ title }}</title>
<style>{{ css }}</style>
{% include "_body.html.j2" %}
